ifconfig -l

# Сетевой интерфейс - это отдельный интерфейс для каждого IP который есть у компьютера. 
# Сетевой интерфейс настроен так, чтобы получать сигналы ТОЛЬКО из определенной сети.
# У компьютера может быть несколько IP, потому что он может быть подключен к нескольким сетям.

# lo0: lo0 - это наверное локалхост. Локалост - это сеть, которая есть на каждом компьютере. 
# lo0: В этой сети находится только оин компьютер - Ваш компьютер. Он используется для тестирования или иногда для связи между приложениями на одном компьютере. 
# lo0: Адрес: (на IPv4 будет 127.0.0.1) или (на IPv6 будет ::1).

# Thunderbolt - этот порты en2, en3, и bridge0.

# Bluetooth PAN - en4

# Полезные команды: "ifconfig -a" показывает все интерфейсы с деталями. 